#!/usr/bin/env zsh

[[ -v commands[ssh-agent] ]] || return 0

function -zconf-ssh-agent-running() {
  [[ -w $SSH_AUTH_SOCK ]] && builtin kill -0 -- $SSH_AGENT_PID 2>/dev/null
}

{
  -zconf-ssh-agent-running && return
  unset SSH_AGENT_PID SSH_AUTH_SOCK

  local env_file=$ZCONF/stickycache/ssh-agent-$EUID
  [[ -r $env_file ]] && builtin source $env_file >/dev/null && -zconf-ssh-agent-running && return

  local tmp=$env_file.tmp.$$
  local -a args
  zstyle -a :zconf:ssh-agent: extra-args args
  command ssh-agent -s $args >$tmp || return
  builtin source $tmp >/dev/null   || return
  -zconf-ssh-agent-running           || return
  zf_mv -f -- $tmp $env_file       || return
} always {
  builtin unfunction -- -zconf-ssh-agent-running
}
